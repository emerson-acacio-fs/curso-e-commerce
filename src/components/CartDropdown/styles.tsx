import styled, {css} from 'styled-components';
import {WrapperCartList} from 'components/CartList/styles';

export const WrapperCartDropdown = styled.div`
  ${WrapperCartList} {
    width: 56rem;
  }
`;
