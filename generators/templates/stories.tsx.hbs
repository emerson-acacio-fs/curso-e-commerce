import {Meta, Story} from '@storybook/react/types-6-0';

import { {{pascalCase name}} } from '.';

const data = {
  title: '{{{pascalCase name}}}',
  component: {{{pascalCase name}}},
  argTypes: {},
} as Meta;

export default data;

export const Defoult: Story = (args) => <{{{pascalCase name}}} {...args} />;

